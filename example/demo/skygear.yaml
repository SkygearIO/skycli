app: blog

deployments:
  express:
    type: http-service
    path: /api
    context: express
    port: 8080
    secrets:
      - MONGO_DB_URL
  frontend:
    type: http-service
    path: /
    context: frontend
    port: 8080

hooks:
  - path: /api/after_user_create
    event: after_user_create
