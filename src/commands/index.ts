import app from "./app";
import auth from "./auth";
import config from "./config";
import secret from "./secret";
import domain from "./domain";
import version from "./version";

export default [app, auth, config, secret, domain, version];
